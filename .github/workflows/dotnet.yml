name: welcome
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: welcome
        run: echo "Welcome!"
        
      - name: restore
        run: dotnet restore
        
      - name: build
        run: dotnet build
      
      - name: test
        run: dotnet test
